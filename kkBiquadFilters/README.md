# DualBiquad Filters

Custom biquad implementation by Pete.

The slope is continously variable between 2nd and 4th order.

The multiband examples are phase aligned crossovers which makes them ideal for multi-band compressors or level adjustments.